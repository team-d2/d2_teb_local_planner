## 1. Trajectory (軌道生成)

### `teb_autosize` (bool, default: true)
**重要度: ★★★★☆**

**意味:**
- TEBの時間分解能を自動調整するかどうか
- `true`: dt±ヒステリシスに基づいて自動的にポーズ数を増減
- `false`: 固定サンプル数で軌道を生成

**アルゴリズムへの影響:**
- TEB (Timed Elastic Band) の時間離散化を動的に管理
- 計算コストと軌道の滑らかさのバランスを自動調整

**変更すべき状況:**
- ✅ 通常は`true`を推奨
- `false`に変更: 計算時間を厳密に制御したい場合

---

### `dt_ref` (double, default: 0.3, unit: seconds)
**重要度: ★★★★★**

**意味:**
- 軌道の時間分解能（隣接ポーズ間の目標時間間隔）

**アルゴリズムへの影響:**
- 小さい値: より細かい軌道、計算コスト増、滑らかな動き
- 大きい値: 粗い軌道、計算コスト減、カクカクした動き

**変更すべき状況:**
- 🔹 **0.2-0.4秒**: 標準的な室内ナビゲーション
- 🔹 **0.1-0.2秒**: 高速・高精度が必要な場合
- 🔹 **0.4-0.6秒**: 低速・計算リソース制限がある場合

---

### `dt_hysteresis` (double, default: 0.1, unit: seconds)
**重要度: ★★★☆☆**

**意味:**
- dt_refに対するヒステリシス幅
- ポーズ間の時間が `dt_ref ± dt_hysteresis` の範囲外になると、自動でポーズを追加/削除

**アルゴリズムへの影響:**
- 小さい値: 頻繁にリサイズ、計算オーバーヘッド増
- 大きい値: リサイズ頻度減、軌道の不均一性増

**変更すべき状況:**
- ✅ 通常は0.1秒（dt_refの30%程度）で十分
- 🔹 計算時間が不安定な場合は増やす

---

### `min_samples` (int, default: 3)
**重要度: ★★★★☆**

**意味:**
- TEBに含まれる最小ポーズ数（スタート + 中間点 + ゴール）

**アルゴリズムへの影響:**
- 3: 最小構成（スタート-中間-ゴール）
- 軌道の最小表現能力を規定

**変更すべき状況:**
- ⚠️ **3未満は設定不可**（数学的に軌道を構成できない）
- 🔹 通常は変更不要

---

### `max_samples` (int, default: 500)
**重要度: ★★★★☆**

**意味:**
- TEBに含まれる最大ポーズ数の上限

**アルゴリズムへの影響:**
- 計算リソースの保護
- 長距離経路での計算爆発を防止

**変更すべき状況:**
- 🔹 **100-200**: 短距離・リアルタイム重視
- 🔹 **500-1000**: 長距離・複雑な経路
- ⚠️ 大きすぎると計算時間が指数的に増加

---

### `global_plan_overwrite_orientation` (bool, default: true)
**重要度: ★★★☆☆**

**意味:**
- グローバルプランの姿勢を上書きするか
- `true`: 経路の接線方向を使用
- `false`: グローバルプランの姿勢をそのまま使用

**アルゴリズムへの影響:**
- 姿勢の初期推定に影響

**変更すべき状況:**
- ✅ `true`推奨（大抵のケース）
- `false`: グローバルプランナーが正確な姿勢を提供している場合

---

### `allow_init_with_backwards_motion` (bool, default: false)
**重要度: ★★☆☆☆**

**意味:**
- 初期軌道生成時に後退を許可するか

**アルゴリズムへの影響:**
- `true`: 狭い場所での脱出性向上
- `false`: 前進優先の自然な動き

**変更すべき状況:**
- 🔹 狭い環境・駐車場: `true`
- 🔹 一般的な移動: `false`

---

### `global_plan_viapoint_sep` (double, default: -1.0, unit: meters)
**重要度: ★★★☆☆**

**意味:**
- グローバルプランから経由点を抽出する間隔
- 負の値: 抽出しない

**アルゴリズムへの影響:**
- グローバルプランへの追従精度に影響

**変更すべき状況:**
- 🔹 **0.3-0.5m**: グローバルプランに厳密に従う
- 🔹 **-1.0**: ローカルプランナーに自由度を与える（推奨）

---

### `via_points_ordered` (bool, default: false)
**重要度: ★☆☆☆☆**

**意味:**
- 経由点を順序通りに通過するか

**アルゴリズムへの影響:**
- `true`: 厳密な順序制約、最適化の柔軟性低下
- `false`: 最適な順序を自動選択

**変更すべき状況:**
- 🔹 特定の順序が必要な場合のみ`true`

---

### `max_global_plan_lookahead_dist` (double, default: 3.0, unit: meters)
**重要度: ★★★★☆**

**意味:**
- グローバルプランの先読み距離

**アルゴリズムへの影響:**
- 小さい値: 局所的な最適化、狭い範囲の経路
- 大きい値: 広域的な最適化、長距離の見通し

**変更すべき状況:**
- 🔹 **1.0-2.0m**: 狭い環境・低速移動
- 🔹 **3.0-5.0m**: 広い環境・高速移動
- 🔹 **5.0m以上**: 長距離計画が必要な場合

---

### `exact_arc_length` (bool, default: false)
**重要度: ★☆☆☆☆**

**意味:**
- 弧長を正確に計算するか（ユークリッド距離 vs 実弧長）

**アルゴリズムへの影響:**
- `true`: 計算コスト増、精度向上（微小）
- `false`: 高速、近似計算

**変更すべき状況:**
- ✅ 通常は`false`で十分

---

### `force_reinit_new_goal_dist` (double, default: 1.0, unit: meters)
**重要度: ★★★☆☆**

**意味:**
- この距離以上ゴールが変化したら軌道を再初期化

**アルゴリズムへの影響:**
- 大きな目標変更に対する反応性

**変更すべき状況:**
- 🔹 **0.5-1.0m**: 標準
- 🔹 **1.5-2.0m**: 頻繁な再初期化を避けたい場合

---

### `force_reinit_new_goal_angular` (double, default: 1.57, unit: radians ≈ 90°)
**重要度: ★★★☆☆**

**意味:**
- この角度以上ゴールの姿勢が変化したら軌道を再初期化

**アルゴリズムへの影響:**
- 姿勢変更に対する感度

**変更すべき状況:**
- 🔹 **π/2 (90°)**: 標準
- 🔹 **π/4 (45°)**: より敏感な反応

---

### `feasibility_check_no_poses` (int, default: 5)
**重要度: ★★★☆☆**

**意味:**
- 実行可能性チェックで検査するポーズ数

**アルゴリズムへの影響:**
- 大きい値: 詳細なチェック、計算コスト増
- 小さい値: 粗いチェック、高速

**変更すべき状況:**
- 🔹 **3-5**: 標準
- 🔹 **10以上**: 厳密な実行可能性が必要な場合

---

### `publish_feedback` (bool, default: false)
**重要度: ★☆☆☆☆ (デバッグ用)**

**意味:**
- 詳細なフィードバック情報を発行するか

**アルゴリズムへの影響:**
- デバッグ情報の出力のみ

**変更すべき状況:**
- 🔹 デバッグ時のみ`true`

---

### `control_look_ahead_poses` (int, default: 2)
**重要度: ★★★★☆**

**意味:**
- 速度コマンド計算時に参照する先読みポーズ数

**アルゴリズムへの影響:**
- 大きい値: より先を見据えた滑らかな制御
- 小さい値: 即応性向上、振動的な動き

**変更すべき状況:**
- 🔹 **1-2**: 標準（1は現在ポーズのみ）
- 🔹 **3-5**: 高速移動時の滑らかさ重視

---

### `control_rate` (double, default: 10.0, unit: Hz)
**重要度: ★★★★★**

**意味:**
- 制御ループの実行周波数

**アルゴリズムへの影響:**
- システム全体のリアルタイム性

**変更すべき状況:**
- 🔹 **5-10 Hz**: 標準的なロボット
- 🔹 **20 Hz以上**: 高速ロボット、高精度制御
- ⚠️ 計算能力を超える周波数は設定しない

---

## 2. Robot (ロボット設定)

### `max_vel_x` (double, default: 0.5, unit: m/s)
**重要度: ★★★★★**

**意味:**
- 前進方向の最大速度

**アルゴリズムへの影響:**
- 軌道の速度プロファイルの上限
- 最適化の目標速度

**変更すべき状況:**
- ⚠️ **必ずロボットの実際の能力に合わせる**
- 🔹 安全マージンを考慮（実能力の80-90%）

---

### `max_vel_x_backwards` (double, default: 0.2, unit: m/s)
**重要度: ★★★☆☆**

**意味:**
- 後退方向の最大速度

**アルゴリズムへの影響:**
- 後退時の動作速度制限

**変更すべき状況:**
- 🔹 通常は前進の30-50%程度

---

### `max_vel_y` (double, default: 0.0, unit: m/s)
**重要度: ★★★☆☆ (オムニロボットのみ)**

**意味:**
- 横方向の最大速度（オムニホイール用）

**アルゴリズムへの影響:**
- 横移動の可否と速度

**変更すべき状況:**
- 🔹 **0.0**: 差動駆動ロボット
- 🔹 **>0.0**: オムニホイールロボット

---

### `max_vel_theta` (double, default: 0.8, unit: rad/s)
**重要度: ★★★★★**

**意味:**
- 最大角速度（回転速度）

**アルゴリズムへの影響:**
- 旋回性能の上限
- カーブでの速度制限

**変更すべき状況:**
- ⚠️ **ロボットの実能力に合わせる**
- 🔹 **0.5-1.0 rad/s**: 一般的な室内ロボット
- 🔹 **1.5-2.0 rad/s**: 高速旋回可能なロボット

---

### `acc_lim_x` (double, default: 0.5, unit: m/s²)
**重要度: ★★★★★**

**意味:**
- 並進方向の最大加速度

**アルゴリズムへの影響:**
- 速度変化の急峻さ
- 動的実行可能性の判定

**変更すべき状況:**
- ⚠️ **ロボットの物理限界より小さく設定**
- 🔹 **0.3-0.5 m/s²**: 標準的なロボット
- 🔹 **1.0 m/s²以上**: 高性能ロボット

---

### `acc_lim_y` (double, default: 0.0, unit: m/s²)
**重要度: ★★☆☆☆ (オムニロボットのみ)**

**意味:**
- 横方向の最大加速度

**アルゴリズムへの影響:**
- 横移動の加速性能

---

### `acc_lim_theta` (double, default: 0.5, unit: rad/s²)
**重要度: ★★★★★**

**意味:**
- 最大角加速度

**アルゴリズムへの影響:**
- 回転動作の滑らかさ
- 急旋回の可否

**変更すべき状況:**
- ⚠️ **ロボットの物理限界に合わせる**
- 🔹 **0.3-0.5 rad/s²**: 標準
- 🔹 **1.0 rad/s²以上**: 俊敏なロボット

---

### `min_turning_radius` (double, default: 0.0, unit: meters)
**重要度: ★★★★☆ (車両型ロボット)**

**意味:**
- 最小旋回半径（Ackermann型ロボット用）
- 0.0 = 差動駆動（その場回転可能）

**アルゴリズムへの影響:**
- 経路の曲率制約
- キネマティックモデルの選択

**変更すべき状況:**
- 🔹 **0.0**: 差動駆動ロボット
- 🔹 **>0.0**: Ackermann型車両（実車の最小旋回半径）

---

### `wheelbase` (double, default: 1.0, unit: meters)
**重要度: ★★★☆☆ (車両型ロボット)**

**意味:**
- 前輪と後輪の距離（車両型ロボット）

**アルゴリズムへの影響:**
- キネマティックモデルの精度

**変更すべき状況:**
- 🔹 車両型ロボットのみ実測値を設定

---

### `cmd_angle_instead_rotvel` (bool, default: false)
**重要度: ★★☆☆☆ (車両型ロボット)**

**意味:**
- 角速度の代わりにステアリング角を出力するか

**アルゴリズムへの影響:**
- 出力コマンドの形式

**変更すべき状況:**
- 🔹 Ackermann型車両で`true`

---

## 3. Goal Tolerance (ゴール許容誤差)

### `xy_goal_tolerance` (double, default: 0.2, unit: meters)
**重要度: ★★★★★**

**意味:**
- ゴール位置への到達判定距離

**アルゴリズムへの影響:**
- ゴール到達の判定基準

**変更すべき状況:**
- 🔹 **0.1-0.2m**: 標準的な精度
- 🔹 **0.05m**: 高精度位置決め
- 🔹 **0.5m**: ラフな到達判定

---

### `yaw_goal_tolerance` (double, default: 0.2, unit: radians ≈ 11.5°)
**重要度: ★★★★☆**

**意味:**
- ゴール姿勢への到達判定角度

**アルゴリズムへの影響:**
- 姿勢の精度要求

**変更すべき状況:**
- 🔹 **0.1-0.2 rad (6-11°)**: 標準
- 🔹 **0.05 rad (3°)**: 高精度
- 🔹 **0.5 rad (30°)**: ラフな姿勢制御

---

### `free_goal_vel` (bool, default: false)
**重要度: ★★★☆☆**

**意味:**
- ゴール到達時に速度をゼロにするか
- `false`: 完全停止を要求
- `true`: ゴール通過を許可（速度維持）

**アルゴリズムへの影響:**
- 最終速度の制約

**変更すべき状況:**
- 🔹 **false**: 通常のゴール到達（停止）
- 🔹 **true**: 経由点として扱う場合

---

## 4. Obstacles (障害物回避)

### `min_obstacle_dist` (double, default: 0.5, unit: meters)
**重要度: ★★★★★**

**意味:**
- 障害物からの最小安全距離

**アルゴリズムへの影響:**
- 障害物回避の基本制約
- コスト関数の主要パラメータ

**変更すべき状況:**
- ⚠️ **ロボットのフットプリント + 安全マージン**
- 🔹 **0.3-0.5m**: 標準的な室内ロボット
- 🔹 **0.1-0.2m**: 狭い環境
- 🔹 **0.8-1.0m**: 安全重視

---

### `inflation_dist` (double, default: 0.6, unit: meters)
**重要度: ★★★★☆**

**意味:**
- 障害物の影響範囲（コスト減衰の外側境界）
- min_obstacle_dist < inflation_dist

**アルゴリズムへの影響:**
- 障害物周辺でのコスト勾配
- 回避動作の滑らかさ

**変更すべき状況:**
- 🔹 **min_obstacle_dist + 0.1-0.2m**: 標準
- 🔹 大きい値: より早期から回避動作開始

---

### `dynamic_obstacle_inflation_dist` (double, default: 0.6, unit: meters)
**重要度: ★★★☆☆**

**意味:**
- 動的障害物用のinflation距離

**アルゴリズムへの影響:**
- 移動障害物への反応範囲

---

### `include_dynamic_obstacles` (bool, default: true)
**重要度: ★★★★☆**

**意味:**
- 動的障害物を考慮するか

**アルゴリズムへの影響:**
- 移動物体への対応

**変更すべき状況:**
- 🔹 **true**: 人や移動ロボットがいる環境
- 🔹 **false**: 静的環境のみ

---

### `include_costmap_obstacles` (bool, default: false)
**重要度: ★★★☆☆**

**意味:**
- コストマップからの障害物を含めるか

**アルゴリズムへの影響:**
- 障害物情報のソース

**変更すべき状況:**
- 🔹 スタンドアロンコンポーネントでは通常`false`
- 🔹 Nav2統合時は`true`

---

### `costmap_obstacles_behind_robot_dist` (double, default: 1.5, unit: meters)
**重要度: ★★☆☆☆**

**意味:**
- ロボット後方で考慮する障害物の距離

**アルゴリズムへの影響:**
- 後退時の安全性

---

### `obstacle_poses_affected` (int, default: 25)
**重要度: ★★★☆☆**

**意味:**
- 各障害物が影響を与えるTEBポーズ数

**アルゴリズムへの影響:**
- 計算効率と精度のトレードオフ
- 大きい値: 詳細な回避、計算コスト増

**変更すべき状況:**
- 🔹 **15-30**: 標準
- 🔹 **50以上**: 複雑な回避が必要な場合

---

### `legacy_obstacle_association` (bool, default: false)
**重要度: ★☆☆☆☆**

**意味:**
- 旧式の障害物関連付けアルゴリズムを使用するか

**アルゴリズムへの影響:**
- 障害物とTEBポーズの関連付け方法

**変更すべき状況:**
- ✅ 通常は`false`（新しいアルゴリズム使用）

---

### `obstacle_association_force_inclusion_factor` (double, default: 1.5)
**重要度: ★★☆☆☆**

**意味:**
- 障害物を強制的に関連付ける距離係数

**アルゴリズムへの影響:**
- 遠い障害物の考慮範囲

**変更すべき状況:**
- 🔹 **1.5-2.0**: 標準

---

### `obstacle_association_cutoff_factor` (double, default: 5.0)
**重要度: ★★☆☆☆**

**意味:**
- 障害物関連付けの最大距離係数

**アルゴリズムへの影響:**
- 計算効率（遠すぎる障害物を無視）

---

## 5. Optimization (最適化)

### `no_inner_iterations` (int, default: 5)
**重要度: ★★★★☆**

**意味:**
- 内側最適化ループの反復回数

**アルゴリズムへの影響:**
- 最適化の精度
- 計算時間に直結

**変更すべき状況:**
- 🔹 **3-5**: 標準（リアルタイム重視）
- 🔹 **10以上**: 高精度重視

---

### `no_outer_iterations` (int, default: 4)
**重要度: ★★★★☆**

**意味:**
- 外側最適化ループの反復回数

**アルゴリズムへの影響:**
- 大域的な最適化の質
- 計算時間への影響大

**変更すべき状況:**
- 🔹 **3-5**: 標準
- 🔹 **1-2**: 計算時間制限が厳しい場合

---

### `optimization_activate` (bool, default: true)
**重要度: ★★★★★**

**意味:**
- 最適化を有効にするか

**アルゴリズムへの影響:**
- `false`にすると初期経路をそのまま使用

**変更すべき状況:**
- ⚠️ 通常は`true`必須

---

### `optimization_verbose` (bool, default: false)
**重要度: ★☆☆☆☆ (デバッグ用)**

**意味:**
- 最適化の詳細ログを出力

---

### `penalty_epsilon` (double, default: 0.1)
**重要度: ★★★☆☆**

**意味:**
- ペナルティ関数の滑らかさパラメータ

**アルゴリズムへの影響:**
- 制約違反に対する罰則の厳しさ
- 小さい値: 厳しいペナルティ、収束性低下
- 大きい値: 緩いペナルティ、制約違反しやすい

**変更すべき状況:**
- 🔹 **0.05-0.1**: 標準
- 🔹 収束問題があれば増やす

---

### Weight パラメータ群
**重要度: ★★★★★ (全体のバランスが重要)**

以下は最適化のコスト関数の重みパラメータです。**相対的なバランスが重要**で、絶対値よりも比率を調整します。

---

#### `weight_max_vel_x` (double, default: 2.0)
**意味:** 最大並進速度への違反ペナルティ

**調整指針:**
- 🔹 大きい値: 速度制限を厳守
- 🔹 小さい値: 他の目標（時間最適化など）を優先

---

#### `weight_max_vel_theta` (double, default: 1.0)
**意味:** 最大角速度への違反ペナルティ

---

#### `weight_acc_lim_x` (double, default: 1.0)
**意味:** 並進加速度制限への違反ペナルティ

**調整指針:**
- 🔹 大きい値: 滑らかな加速、乗り心地重視
- 🔹 小さい値: 急加速許容、応答性重視

---

#### `weight_acc_lim_theta` (double, default: 1.0)
**意味:** 角加速度制限への違反ペナルティ

---

#### `weight_kinematics_nh` (double, default: 1000.0)
**意味:** 非ホロノミック制約（差動駆動の運動学）違反ペナルティ

**調整指針:**
- ⚠️ **差動駆動ロボットでは非常に重要**
- 🔹 **1000.0**: 標準（高い値で制約を厳守）
- 🔹 オムニホイールでは低く設定

---

#### `weight_kinematics_forward_drive` (double, default: 1.0)
**意味:** 前進優先の重み

**調整指針:**
- 🔹 大きい値: 後退を避ける
- 🔹 **1.0-10.0**: 標準

---

#### `weight_kinematics_turning_radius` (double, default: 1.0)
**意味:** 最小旋回半径制約の重み（Ackermann型用）

---

#### `weight_optimaltime` (double, default: 1.0)
**意味:** 移動時間最小化の重み

**調整指針:**
- ⚠️ **必ず > 0 に設定**（ゼロだと無限に遅い経路になる）
- 🔹 **1.0-2.0**: 標準
- 🔹 大きい値: 時間最適化を強く優先

---

#### `weight_shortest_path` (double, default: 0.0)
**意味:** 経路長最小化の重み

**調整指針:**
- 🔹 **0.0**: 時間最適化優先（通常推奨）
- 🔹 **0.1-1.0**: 距離も考慮

---

#### `weight_obstacle` (double, default: 50.0)
**重要度: ★★★★★**

**意味:** 障害物回避の重み

**調整指針:**
- ⚠️ **非常に重要なパラメータ**
- 🔹 **30.0-100.0**: 標準
- 🔹 大きい値: 安全マージン増、回避動作が大胆
- 🔹 小さい値: リスク許容、障害物に接近しやすい

---

#### `weight_inflation` (double, default: 0.1)
**意味:** inflation領域への侵入ペナルティ

**調整指針:**
- 🔹 weight_obstacleの1-10%程度

---

#### `weight_dynamic_obstacle` (double, default: 10.0)
**意味:** 動的障害物回避の重み

---

#### `weight_dynamic_obstacle_inflation` (double, default: 0.1)
**意味:** 動的障害物のinflation領域ペナルティ

---

#### `weight_viapoint` (double, default: 1.0)
**意味:** 経由点への追従重み

**調整指針:**
- 🔹 **1.0-50.0**: 経由点の重要度に応じて
- 🔹 大きい値: 厳密に経由点を通過

---

#### `weight_adapt_factor` (double, default: 2.0)
**意味:** 動的重み調整の係数

**調整指針:**
- 🔹 最適化の安定性調整用
- 🔹 通常は変更不要

---

## 6. Homotopy Class Planner (複数経路探索)

### `enable_homotopy_class_planning` (bool, default: false)
**重要度: ★★★★☆**

**意味:**
- 複数の異なる経路候補（ホモトピークラス）を探索するか

**アルゴリズムへの影響:**
- `true`: 障害物の左右など複数経路を並列評価
- `false`: 単一経路のみ最適化（高速）

**変更すべき状況:**
- 🔹 **false**: 計算リソース制限、シンプルな環境
- 🔹 **true**: 複雑な環境、最適経路選択が重要な場合

**計算コスト:**
- ⚠️ 有効時は計算時間が数倍に増加

---

### `enable_multithreading` (bool, default: true)
**重要度: ★★★☆☆**

**意味:**
- ホモトピークラスの並列計算を有効化

**アルゴリズムへの影響:**
- マルチコアCPUで計算高速化

**変更すべき状況:**
- ✅ 通常は`true`

---

### `simple_exploration` (bool, default: false)
**重要度: ★★☆☆☆**

**意味:**
- シンプルな探索戦略を使用

**アルゴリズムへの影響:**
- `true`: 高速だが粗い探索
- `false`: 詳細だが計算コスト大

---

### `max_number_classes` (int, default: 5)
**重要度: ★★★★☆**

**意味:**
- 同時に評価する経路候補の最大数

**アルゴリズムへの影響:**
- 計算時間に線形に影響

**変更すべき状況:**
- 🔹 **3-5**: 標準
- 🔹 **10以上**: 複雑な環境
- ⚠️ 大きすぎると計算時間超過

---

### `selection_cost_hysteresis` (double, default: 5.0)
**重要度: ★★★☆☆**

**意味:**
- 経路切り替えのヒステリシス（揺らぎ防止）

**アルゴリズムへの影響:**
- 大きい値: 経路変更を抑制、安定性向上
- 小さい値: 頻繁に最適経路に切り替え

**変更すべき状況:**
- 🔹 **1.0-5.0**: 標準
- 🔹 経路がチャタリングする場合は増やす

---

### `selection_prefer_initial_plan` (double, default: 1.0)
**重要度: ★★☆☆☆**

**意味:**
- 初期プランへの選好度（0-1）

**アルゴリズムへの影響:**
- 大きい値: グローバルプランを尊重

---

### `selection_obst_cost_scale` (double, default: 1.0)
**意味:** 経路選択時の障害物コストのスケール

---

### `selection_viapoint_cost_scale` (double, default: 1.0)
**意味:** 経路選択時の経由点コストのスケール

---

### `selection_alternative_time_cost` (bool, default: false)
**意味:** 代替経路の時間コストを考慮するか

---

### `roadmap_graph_no_samples` (int, default: 15)
**重要度: ★★★☆☆**

**意味:**
- ロードマップグラフのサンプル点数

**アルゴリズムへの影響:**
- 探索空間の密度

**変更すべき状況:**
- 🔹 **10-20**: 標準
- 🔹 **30以上**: 複雑な環境

---

### `roadmap_graph_area_width` (double, default: 5.0, unit: meters)
**重要度: ★★★☆☆**

**意味:**
- ロードマップ生成の範囲幅

**アルゴリズムへの影響:**
- 探索する横方向の範囲

---

### `h_signature_prescaler` (double, default: 1.0)
**重要度: ★★☆☆☆**

**意味:**
- H-signature計算のスケール係数

**アルゴリズムへの影響:**
- 経路の類似性判定の感度

---

### `h_signature_threshold` (double, default: 0.1)
**重要度: ★★★☆☆**

**意味:**
- 異なる経路と判定するH-signatureのしきい値

**アルゴリズムへの影響:**
- 小さい値: より多くの経路候補を生成
- 大きい値: 似た経路を統合

**変更すべき状況:**
- 🔹 **0.1**: 標準
- 🔹 経路候補が多すぎる場合は増やす

---

### `obstacle_heading_threshold` (double, default: 0.45, unit: radians ≈ 26°)
**重要度: ★★☆☆☆**

**意味:**
- 障害物の「前方」判定の角度しきい値

---

### `viapoints_all_candidates` (bool, default: true)
**意味:** すべての経路候補で経由点を考慮するか

---

### `visualize_hc_graph` (bool, default: false)
**意味:** ホモトピークラスグラフを可視化するか（デバッグ用）

---

## 7. Footprint Model (ロボット形状)

### `footprint_model.type` (string, default: "point")
**重要度: ★★★★★**

**選択肢:**
- `"point"`: 点ロボット（最も単純）
- `"circular"`: 円形ロボット
- `"polygon"`: 多角形ロボット（最も正確）

**変更すべき状況:**
- 🔹 **point**: テスト・デバッグ用
- 🔹 **circular**: 円形に近似できるロボット（推奨）
- 🔹 **polygon**: 複雑な形状の正確な回避が必要

---

### `footprint_model.radius` (double, default: 0.3, unit: meters)
**重要度: ★★★★★ (circular型の場合)**

**意味:**
- 円形ロボットの半径

**変更すべき状況:**
- ⚠️ **ロボットの実際の大きさに合わせる**
- 🔹 外接円半径を使用（安全マージン込み）

---

### `footprint_model.vertices` (double[], unit: meters)
**重要度: ★★★★☆ (polygon型の場合)**

**意味:**
- 多角形頂点の座標リスト [x1, y1, x2, y2, ...]

---

## 8. パラメータチューニングガイド

### 初期セットアップ（必須）

1. **ロボット物理パラメータ** 最優先
   ```yaml
   max_vel_x: <実測値の80-90%>
   max_vel_theta: <実測値の80-90%>
   acc_lim_x: <実測値の70-80%>
   acc_lim_theta: <実測値の70-80%>
   footprint_model.type: "circular"
   footprint_model.radius: <外接円半径 + 5cm>
   ```

2. **基本動作パラメータ**
   ```yaml
   control_rate: 10.0  # ロボットの能力に応じて
   dt_ref: 0.3
   min_obstacle_dist: <radius + 0.2>
   inflation_dist: <min_obstacle_dist + 0.1>
   ```

3. **最適化の基本重み**
   ```yaml
   weight_optimaltime: 1.0  # 必須 > 0
   weight_obstacle: 50.0
   weight_kinematics_nh: 1000.0  # 差動駆動のみ
   ```

---

### 問題別チューニング

#### 問題: 障害物に近づきすぎる
```yaml
min_obstacle_dist: 0.5 → 0.7  # 増やす
weight_obstacle: 50.0 → 100.0  # 増やす
```

#### 問題: 障害物を大回りしすぎる
```yaml
min_obstacle_dist: 0.5 → 0.3  # 減らす
weight_obstacle: 50.0 → 30.0  # 減らす
```

#### 問題: 動きがカクカクする
```yaml
dt_ref: 0.3 → 0.2  # 細かく
weight_acc_lim_x: 1.0 → 2.0  # 増やす
weight_acc_lim_theta: 1.0 → 2.0  # 増やす
```

#### 問題: 動きが遅すぎる
```yaml
weight_optimaltime: 1.0 → 2.0  # 増やす
weight_shortest_path: 0.0 → 0.5  # 追加
```

#### 問題: 計算時間がかかりすぎる
```yaml
max_samples: 500 → 200  # 減らす
no_inner_iterations: 5 → 3  # 減らす
no_outer_iterations: 4 → 2  # 減らす
enable_homotopy_class_planning: true → false
```

#### 問題: ゴール付近で振動する
```yaml
xy_goal_tolerance: 0.2 → 0.3  # 緩める
yaw_goal_tolerance: 0.2 → 0.3  # 緩める
control_look_ahead_poses: 2 → 3  # 増やす
```

#### 問題: 経路が頻繁に切り替わる（ホモトピー使用時）
```yaml
selection_cost_hysteresis: 5.0 → 10.0  # 増やす
h_signature_threshold: 0.1 → 0.2  # 増やす
```

---

### 環境別推奨設定

#### 狭い室内環境
```yaml
max_vel_x: 0.3
dt_ref: 0.2
min_obstacle_dist: 0.3
max_global_plan_lookahead_dist: 2.0
```

#### 広い屋外環境
```yaml
max_vel_x: 1.0
dt_ref: 0.4
min_obstacle_dist: 0.5
max_global_plan_lookahead_dist: 5.0
```

#### 動的環境（人が多い）
```yaml
include_dynamic_obstacles: true
weight_dynamic_obstacle: 20.0
control_rate: 20.0  # 高頻度更新
```

---

### 重要な注意事項

1. **重みパラメータは相対値**: 全体のスケールより比率が重要
2. **計算時間の監視**: リアルタイム性を常に確認
3. **段階的調整**: 一度に1-2個のパラメータのみ変更
4. **実機テスト必須**: シミュレーションと実機で挙動が異なる
5. **安全マージン**: 物理限界の80-90%で設定

---

### デバッグ時の有用なパラメータ

```yaml
optimization_verbose: true  # 最適化の詳細ログ
publish_feedback: true      # フィードバック情報
visualize_hc_graph: true    # ホモトピーグラフ可視化
```